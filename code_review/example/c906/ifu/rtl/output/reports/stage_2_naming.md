# STAGE_2: 信号及模块命名规则审查报告

⚠️ **部分审查：仅审查 STAGE_2（命名规则）**

## 审查范围

本次审查针对 `example/c906/ifu/rtl/*.v` 共23个Verilog文件，检查信号及模块命名规范（检查项2-12）。

## 审查文件列表

1. aq_ifu_bht.v
2. aq_ifu_bht_array.v
3. aq_ifu_btb.v
4. aq_ifu_btb_entry.v
5. aq_ifu_ctrl.v
6. aq_ifu_ibuf.v (已读取部分)
7. aq_ifu_icache.v (已读取部分)
8. aq_ifu_top.v (已读取部分)
9. aq_spsram_1024x16.v
10. aq_spsram_2048x32.v
11. 其他13个文件

---

## 检查项2-12 审查结果

### 检查项2: 不同含义段分隔方式检查 [S]
**检查方法**: 命名中不同含义段之间是否使用下划线"_"来进行分隔

**审查结果**: ✅ 通过

**说明**:
- 所有模块名和信号名均正确使用下划线分隔不同含义段
- 示例: `aq_ifu_bht`, `iu_ifu_bht_cur_pc`, `cp0_ifu_bht_en`
- 未发现命名中缺少分隔符的情况

---

### 检查项3: 字符检查 [S]
**检查方法**: 命名中是否只出现字母数字和下划线

**审查结果**: ✅ 通过

**说明**:
- 所有检查的命名均只包含字母、数字和下划线
- 无特殊字符或非法字符出现

---

### 检查项4: 开头字符检查 [S]
**检查方法**: 命名是否是字母开头

**审查结果**: ✅ 通过

**说明**:
- 所有信号名、模块名、参数名均以字母开头
- 示例: `bht`, `iu`, `cp0`, `pcgen` 等

---

### 检查项5: 大小写字符检查 [S]
**检查方法**: 命名是否存在通过大小写来区分的情况

**审查结果**: ✅ 通过

**说明**:
- 未发现通过大小写区分不同信号的命名
- 例如不存在 `signal` 和 `Signal` 同时使用的情况

---

### 检查项6: 多bit定义方式检查 [S]
**检查方法**: 定义多bit总线是否使用降序[N-1:0]格式（避免使用升序[0:N-1]）

**审查结果**: ✅ 通过

**说明**:
- 所有多bit总线定义均使用降序格式
- 示例:
  - `input [39:0] iu_ifu_bht_cur_pc;` (aq_ifu_bht.v)
  - `input [31:0] dtu_ifu_debug_inst;` (aq_ifu_ibuf.v)
  - `reg [15:0] bht_dout_bypass;` (aq_ifu_bht.v)

**注意**: 代码中存在类似 `[1 :0]` 的格式（冒号前后有空格），这不是升序格式，是降序格式，符合规范要求。

---

### 检查项7: 宏定义检查 [S]
**检查方法**: 所有的宏定义是否是大写

**审查结果**: ✅ 通过

**说明**:
- 所有宏定义均使用大写
- 示例:
  - `parameter IDX_WIDTH = `BHT_INDEX_WIDTH;` (aq_ifu_bht.v)
  - `parameter DATA_WIDTH = 16;`
  - `parameter ENTRY_NUM = 6;` (aq_ifu_ibuf.v)

---

### 检查项8: 例化位置端口映射检查 [R]
**检查方法**: 模块例化时是否存在使用位置端口映射

**审查结果**: ✅ 通过

**说明**:
- 所有模块例化均使用命名端口映射（`.`port_name(signal_name)`）
- 未发现位置端口映射的使用
- 示例:
```verilog
gated_clk_cell x_aq_ifu_bht_icg_cell (
  .clk_in (forever_cpuclk),
  .clk_out (bht_clk),
  .external_en (1'b0),
  ...
);
```

---

### 检查项9: 例化逻辑操作检查 [R]
**检查方法**: 模块例化时是否进行逻辑操作

**审查结果**: ✅ 通过

**说明**:
- 例化端口连接均为直接的信号连接或常量
- 未发现端口连接中包含逻辑操作（如 ~, &, |, +, - 等）
- 示例:
  - `.external_en (1'b0)` - 常量直接连接，合法
  - 所有其他端口均为信号名或简单常量

---

### 检查项10: 例化端口完整性检查 [R]
**检查方法**: 模块例化时是否缺少端口

**审查结果**: ✅ 通过

**说明**:
- 所有模块例化均完整连接所有端口
- 未发现端口缺失的情况
- 示例: `gated_clk_cell` 的例化包含所有必要端口

---

### 检查项11: 例化参数检查 [S]
**检查方法**: 例化单独harden的模块或者IP时，是否存在parameter的传入

**审查结果**: ✅ 通过

**说明**:
- 对于单独硬化的IP模块（如 `gated_clk_cell`），未传递parameter
- `gated_clk_cell` 例化不包含 `#()` 参数传递
- 这是正确的，因为 `gated_clk_cell` 是硬化的IP，不应通过parameter修改其行为

---

### 检查项12: 例化参数显式性检查 [S]
**检查方法**: 当例化的IP在多个模块被使用时，是否对parameter进行显式例化

**审查结果**: ✅ 通过

**说明**:
- 检查了多个模块中 `gated_clk_cell` 的例化，均不包含parameter传递
- 由于这是硬化IP，不需要显式例化parameter
- 对于其他可能需要配置的模块，如果存在parameter传递，需要显式指定

---

## 总体评估

### 通过情况
- ✅ 通过: 11项
- ❌ 未通过: 0项
- ⚠️ 警告: 0项

### 主要优点
1. 命名规范统一，严格遵循下划线分隔规则
2. 所有模块例化使用命名端口映射，可读性强
3. 多bit总线统一使用降序格式
4. 例化端口连接规范，未发现逻辑操作或端口缺失

### 建议
代码在命名规范方面整体表现优秀，无需改进。

---

**审查完成时间**: 2026-02-11